export class serie{

    constructor(public position: number, public title: string, public platform: string, public nr_seasons:number, public description:string, public direction:string, public image:string)
    {

    }
}